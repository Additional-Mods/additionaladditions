package one.dqu.additionaladditions.misc;

public interface AlbumJukeboxExtension {
    int additionaladditions$getTrack();
    void additionaladditions$setTrack(int track);

    boolean additionaladditions$isPlaying();
    void additionaladditions$setPlaying(boolean playing);
}
