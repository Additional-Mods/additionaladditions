package one.dqu.additionaladditions.feature.glint;

import net.minecraft.world.item.DyeColor;
import org.jetbrains.annotations.Nullable;

public interface GlintColorHolder {
    @Nullable DyeColor additionaladditions$getGlintColor();
    void additionaladditions$setGlintColor(@Nullable DyeColor color);
}
